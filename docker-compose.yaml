services:
  karretje:
    build: .
    container_name: karretje-dev
    ports:
      - "6969:80"
    volumes:
      - ./server/data:/app/server/data
    environment:
      - API_KEY=${API_KEY:-dev-key-change-in-production}
